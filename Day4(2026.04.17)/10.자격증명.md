# 10. auth

### application auth

- Secret : application 인증을 위한 1회성 인증토큰
- clouds.yaml 은 여러 클라우드에서 다양하게 사용할 수 있음.

```
    # OS_CLOUD=openstack or --os-cloud=openstack
        clouds:
        openstack:
        auth:
        auth_url: http://172.25.250.50:5000
        application_credential_id: "4392edbe4f8843c8af7b3cece6d89639"
        application_credential_secret: "Umx6R_vrDpqFW4WoXb7rsD-4672reni9qZkcfpQgFWXllavc6HDfg"
        region_name: "regionOne"
        interface: "public"
        identity_api_version: 3
        auth_type: "v3applicationcredential"

```

openstack을 쓰는 경우엔 OS_CLOUD에 openstack이라고 설정하면됨.

### openstack update

- down time 이 변경되는 경우
  flavor 변경, image 변경, subnet시간 변경 

